# Machine Learning Phishing Detection Extension

An end-to-end machine learning system paired with a browser extension to classify and detect phishing websites in real-time.

## Project Structure
- `Dataset/`: Contains sanitized training data (PhiUSIIL Phishing URL Dataset)
- `src/`: Source code for model training and preprocessing
- `requirements.txt`: Environment dependencies
- `.gitignore`: Files excluded from version control
